<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Email - Share Reads</title>
    <script src="https://kit.fontawesome.com/b795da5b50.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="css/auth-modern.css">
    <link rel="icon" href="images/book.png" type="image/png">
</head>
<body>
    <div class="auth-container">
        <div class="auth-card">
            <div class="auth-logo">
                <img src="images/Share Reads .png" alt="Share Reads Logo">
            </div>
            
            <div style="text-align: center; margin-bottom: 32px;">
                <div style="width: 80px; height: 80px; background: linear-gradient(135deg, #8b7fc7 0%, #a89fd9 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 20px; box-shadow: 0 8px 24px rgba(139, 127, 199, 0.3);">
                    <i class="fa-solid fa-envelope-open-text" style="font-size: 36px; color: white;"></i>
                </div>
                <h1 class="auth-title">Verify Your Email</h1>
                <p class="auth-subtitle">We've sent a verification code to your email address. Please enter it below to continue.</p>
            </div>

            <% if (typeof errorMessage != "undefined") { %>
                <div class="error-message">
                    <i class="fa-solid fa-circle-exclamation"></i> <%= errorMessage %>
                </div>
            <% } %>
            
            <form class="auth-form" action="/verifyEmail" method="post">
                <div class="form-group">
                    <div class="input-wrapper">
                        <span class="input-icon">
                            <i class="fa-solid fa-key"></i>
                        </span>
                        <input type="text" class="form-input" placeholder="Enter verification code" name="code" required autofocus>
                    </div>
                    <small style="display: block; margin-top: 8px; color: #8e8ea7; font-size: 13px; text-align: center;">
                        <i class="fa-solid fa-info-circle"></i> Check your email inbox or spam folder
                    </small>
                </div>

                <div class="btn-group">
                    <button type="submit" class="btn-auth btn-primary">
                        <i class="fa-solid fa-check-circle"></i> Verify & Continue
                    </button>
                </div>
            </form>

            <div class="auth-footer">
                Didn't receive the code? <a href="/signup">Resend Code</a>
            </div>
        </div>
    </div>
</body>
</html>