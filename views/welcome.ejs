<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Share Reads</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="icon" href="images/book.png" type="image/png">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 20%, #ffc3d7 50%, #d4a5d9 80%, #e0c3fc 100%);
            background-attachment: fixed;
            min-height: 100vh;
            overflow-x: hidden;
        }
        
        /* Animated Background Shapes */
        .bg-shapes {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 0;
            pointer-events: none;
        }
        
        .shape {
            position: absolute;
            opacity: 0.1;
            animation: float 20s infinite ease-in-out;
        }
        
        .shape-1 {
            top: 10%;
            left: 10%;
            width: 100px;
            height: 100px;
            background: #8b7fc7;
            border-radius: 50%;
            animation-delay: 0s;
        }
        
        .shape-2 {
            top: 60%;
            right: 15%;
            width: 150px;
            height: 150px;
            background: #ffb7d5;
            border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
            animation-delay: 2s;
        }
        
        .shape-3 {
            bottom: 10%;
            left: 20%;
            width: 80px;
            height: 80px;
            background: #a89fd9;
            border-radius: 20px;
            transform: rotate(45deg);
            animation-delay: 4s;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-30px) rotate(10deg); }
        }
        
        /* Public Navigation Bar */
        .public-navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            padding: 20px 0;
            box-shadow: 0 4px 24px rgba(139, 127, 199, 0.08);
            z-index: 1000;
            border-bottom: 1px solid rgba(255, 255, 255, 0.5);
        }
        
        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 32px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .nav-logo {
            font-size: 24px;
            font-weight: 700;
            color: #8b7fc7;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: transform 0.3s ease;
        }
        
        .nav-logo:hover {
            transform: scale(1.05);
        }
        
        .nav-buttons {
            display: flex;
            gap: 16px;
            align-items: center;
        }
        
        .btn-nav {
            padding: 12px 28px;
            border-radius: 14px;
            font-weight: 700;
            text-decoration: none;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            display: inline-flex;
            align-items: center;
            gap: 8px;
            font-size: 15px;
            letter-spacing: 0.3px;
        }
        
        .btn-login {
            color: #8b7fc7;
            background: rgba(255, 255, 255, 0.9);
            border: 2px solid rgba(139, 127, 199, 0.2);
        }
        
        .btn-login:hover {
            background: white;
            border-color: #8b7fc7;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(139, 127, 199, 0.2);
        }
        
        .btn-signup {
            color: white;
            background: linear-gradient(135deg, #8b7fc7 0%, #a89fd9 100%);
            box-shadow: 0 4px 16px rgba(139, 127, 199, 0.3);
        }
        
        .btn-signup:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 24px rgba(139, 127, 199, 0.4);
        }
        
        /* Hero Section - Split Layout */
        .hero-wrapper {
            position: relative;
            z-index: 1;
            min-height: 100vh;
            padding-top: 80px;
        }
        
        .hero-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 60px 32px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
            min-height: calc(100vh - 80px);
        }
        
        .hero-content-left {
            animation: slideInLeft 0.8s ease both;
        }
        
        .hero-badge {
            display: inline-block;
            padding: 10px 24px;
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-radius: 50px;
            font-size: 14px;
            font-weight: 600;
            color: white;
            margin-bottom: 24px;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .hero-main-title {
            font-family: 'Poppins', sans-serif;
            font-size: 56px;
            font-weight: 800;
            line-height: 1.2;
            color: white;
            margin-bottom: 24px;
            text-shadow: 0 4px 20px rgba(107, 91, 140, 0.3);
        }
        
        .hero-main-title .highlight {
            background: linear-gradient(135deg, #4a4a68 0%, #6b5b8c 50%, #8b7fc7 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: none;
            display: inline-block;
        }
        
        .hero-description {
            font-size: 18px;
            line-height: 1.7;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 40px;
            max-width: 540px;
        }
        
        .hero-buttons {
            display: flex;
            gap: 20px;
            margin-bottom: 48px;
            flex-wrap: wrap;
        }
        
        .hero-stats {
            display: flex;
            gap: 48px;
            flex-wrap: wrap;
        }
        
        .stat-item {
            text-align: left;
        }
        
        .stat-number {
            font-size: 44px;
            font-weight: 800;
            color: white;
            line-height: 1;
            margin-bottom: 8px;
            text-shadow: 0 4px 16px rgba(107, 91, 140, 0.3);
            letter-spacing: -1px;
        }
        
        .stat-label {
            font-size: 15px;
            color: rgba(255, 255, 255, 0.88);
            font-weight: 600;
            letter-spacing: 0.3px;
        }
        
        .hero-image-right {
            position: relative;
            animation: slideInRight 0.8s ease both;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        
        .hero-image-wrapper {
            position: relative;
            width: 100%;
            max-width: 600px;
        }
        
        .hero-image-wrapper::before {
            content: '';
            position: absolute;
            top: -24px;
            right: -24px;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(107, 91, 140, 0.4), rgba(139, 127, 199, 0.3));
            border-radius: 32px;
            z-index: -1;
            filter: blur(2px);
        }
        
        .hero-image-wrapper img {
            width: 100%;
            height: auto;
            border-radius: 28px;
            box-shadow: 0 24px 72px rgba(107, 91, 140, 0.35), 0 8px 24px rgba(0, 0, 0, 0.15);
            display: block;
            border: 4px solid rgba(255, 255, 255, 0.6);
        }
        
        .floating-card {
            position: absolute;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            padding: 16px 24px;
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(107, 91, 140, 0.2);
            animation: floatCard 3s ease-in-out infinite;
        }
        
        .floating-card-1 {
            top: 10%;
            right: -30px;
            animation-delay: 0s;
        }
        
        .floating-card-2 {
            bottom: 15%;
            left: -30px;
            animation-delay: 1.5s;
        }
        
        .card-content {
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .card-icon {
            width: 48px;
            height: 48px;
            background: linear-gradient(135deg, #6b5b8c 0%, #8b7fc7 100%);
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 20px;
            box-shadow: 0 4px 16px rgba(107, 91, 140, 0.3);
        }
        
        .card-text h4 {
            font-size: 15px;
            font-weight: 800;
            color: #3a3a4a;
            margin-bottom: 3px;
            letter-spacing: 0.3px;
        }
        
        .card-text p {
            font-size: 13px;
            color: #6e6e8a;
            font-weight: 500;
        }
        
        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        @keyframes floatCard {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-15px); }
        }
        
        .hero-cta-primary {
            transition: all 0.3s ease;
        }
        
        .hero-cta-primary:hover {
            transform: translateY(-4px) scale(1.02);
            box-shadow: 0 12px 40px rgba(139, 127, 199, 0.5) !important;
        }
        
        .hero-cta-secondary {
            transition: all 0.3s ease;
        }
        
        .hero-cta-secondary:hover {
            transform: translateY(-4px) scale(1.02);
            background: white !important;
            box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2) !important;
        }
        
        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            font-size: 24px;
            color: #8b7fc7;
            cursor: pointer;
        }
        
        /* Responsive Design */
        @media (max-width: 1024px) {
            .hero-main-title {
                font-size: 48px;
            }
            
            .hero-container {
                gap: 40px;
            }
        }
        
        @media (max-width: 768px) {
            .nav-buttons {
                position: fixed;
                top: 70px;
                left: 0;
                right: 0;
                background: rgba(255, 255, 255, 0.98);
                flex-direction: column;
                padding: 20px;
                box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
                transform: translateY(-100%);
                opacity: 0;
                transition: all 0.3s ease;
                pointer-events: none;
            }
            
            .nav-buttons.active {
                transform: translateY(0);
                opacity: 1;
                pointer-events: all;
            }
            
            .mobile-menu-btn {
                display: block;
            }
            
            .btn-nav {
                width: 100%;
                justify-content: center;
            }
            
            .hero-container {
                grid-template-columns: 1fr;
                gap: 48px;
                padding: 40px 24px;
            }
            
            .hero-main-title {
                font-size: 36px;
            }
            
            .hero-description {
                font-size: 18px;
            }
            
            .hero-image-right {
                order: -1;
            }
            
            .floating-card-1,
            .floating-card-2 {
                display: none;
            }
            
            .hero-stats {
                gap: 32px;
            }
        }
        
        @media (max-width: 480px) {
            .hero-main-title {
                font-size: 32px;
            }
            
            .hero-description {
                font-size: 16px;
            }
            
            .hero-buttons {
                flex-direction: column;
            }
            
            .hero-buttons a {
                width: 100%;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <!-- Animated Background Shapes -->
    <div class="bg-shapes">
        <div class="shape shape-1"></div>
        <div class="shape shape-2"></div>
        <div class="shape shape-3"></div>
    </div>

    <!-- Public Navigation Bar -->
    <nav class="public-navbar">
        <div class="nav-container">
            <a href="/" class="nav-logo">
                <img src="images/Share Reads .png" alt="Share Reads Logo" style="height: 40px;">
            </a>
            <button class="mobile-menu-btn" id="mobileMenuBtn">
                <i class="fa-solid fa-bars"></i>
            </button>
            <div class="nav-buttons" id="navButtons">
                <a href="/login" class="btn-nav btn-login">
                    <i class="fa-solid fa-sign-in-alt"></i>
                    Login
                </a>
                <a href="/signup" class="btn-nav btn-signup">
                    <i class="fa-solid fa-user-plus"></i>
                    Sign Up
                </a>
            </div>
        </div>
    </nav>

    <!-- Hero Section - Split Layout -->
    <section class="hero-wrapper">
        <div class="hero-container">
            <!-- Left Side - Content -->
            <div class="hero-content-left">
                <div class="hero-badge">
                    <i class="fa-solid fa-sparkles"></i> Welcome to Share Reads
                </div>
                
                <h1 class="hero-main-title">
                    Give your old books a <span class="highlight">new life!</span>
                </h1>
                
                <p class="hero-description">
                    Join our community of book lovers. Share, discover, and borrow books with ease. Connect with fellow readers and make your books available to those who'll cherish them.
                </p>
                
                <div style="display: flex; flex-direction: column; gap: 16px; align-items: flex-start;">
                    <button onclick="demoLogin()" class="hero-cta-demo" style="font-size: 18px; padding: 18px 48px; text-decoration: none; background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%); color: #8b4513; backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px); box-shadow: 0 4px 16px rgba(252, 182, 159, 0.3); font-weight: 700; border: 2px solid rgba(255, 255, 255, 0.6); border-radius: 16px; display: inline-flex; align-items: center; gap: 10px; cursor: pointer; transition: all 0.3s ease;">
                        <i class="fa-solid fa-eye"></i> Try Demo Account
                    </button>
                    
                    <div class="hero-buttons">
                        <a href="/signup" class="hero-cta-primary" style="font-size: 18px; padding: 18px 48px; text-decoration: none; background: linear-gradient(135deg, #8b7fc7 0%, #a89fd9 100%); box-shadow: 0 8px 32px rgba(139, 127, 199, 0.4); font-weight: 700; border-radius: 16px; color: white; display: inline-flex; align-items: center; gap: 10px;">
                            <i class="fa-solid fa-rocket"></i> Get Started Free
                        </a>
                        <a href="/login" class="hero-cta-secondary" style="font-size: 18px; padding: 18px 48px; text-decoration: none; background: rgba(255, 255, 255, 0.9); color: #8b7fc7; backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px); box-shadow: 0 4px 16px rgba(139, 127, 199, 0.2); font-weight: 700; border: 2px solid rgba(255, 255, 255, 0.5); border-radius: 16px; display: inline-flex; align-items: center; gap: 10px;">
                            <i class="fa-solid fa-sign-in-alt"></i> Login
                        </a>
                    </div>
                </div>
                
                <p style="margin-top: 16px; font-size: 14px; color: rgba(255, 255, 255, 0.85); font-weight: 500; display: flex; align-items: center; gap: 8px;">
                    <i class="fa-solid fa-info-circle"></i> Try demo account - no registration required
                </p>
                
                <!-- Hidden Demo Login Form -->
                <form id="demoLoginForm" action="/login" method="POST" style="display: none;">
                    <input type="hidden" name="username" value="nawrinjahanome@gmail.com">
                    <input type="hidden" name="password" value="11111111">
                </form>
                
                <div class="hero-stats">
                    <div class="stat-item">
                        <div class="stat-number">1000+</div>
                        <div class="stat-label">Books Shared</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">500+</div>
                        <div class="stat-label">Happy Readers</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">8</div>
                        <div class="stat-label">Divisions Covered</div>
                    </div>
                </div>
            </div>
            
            <!-- Right Side - Image -->
            <div class="hero-image-right">
                <div class="hero-image-wrapper">
                    <img src="images/Shelf.png" alt="Book Shelf" />
                    
                    <!-- Floating Card 1 -->
                    <div class="floating-card floating-card-1">
                        <div class="card-content">
                            <div class="card-icon">
                                <i class="fa-solid fa-book"></i>
                            </div>
                            <div class="card-text">
                                <h4>Easy Sharing</h4>
                                <p>List your books instantly</p>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Floating Card 2 -->
                    <div class="floating-card floating-card-2">
                        <div class="card-content">
                            <div class="card-icon">
                                <i class="fa-solid fa-users"></i>
                            </div>
                            <div class="card-text">
                                <h4>Community</h4>
                                <p>Connect with readers</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script>
        // Mobile menu toggle
        const mobileMenuBtn = document.getElementById('mobileMenuBtn');
        const navButtons = document.getElementById('navButtons');
        
        mobileMenuBtn.addEventListener('click', function() {
            navButtons.classList.toggle('active');
            const icon = this.querySelector('i');
            if (navButtons.classList.contains('active')) {
                icon.classList.remove('fa-bars');
                icon.classList.add('fa-times');
            } else {
                icon.classList.remove('fa-times');
                icon.classList.add('fa-bars');
            }
        });
        
        // Demo login function
        function demoLogin() {
            document.getElementById('demoLoginForm').submit();
        }
        
        // Add hover effect to demo button
        document.querySelector('.hero-cta-demo').addEventListener('mouseenter', function() {
            this.style.transform = 'translateY(-2px) scale(1.02)';
            this.style.boxShadow = '0 8px 24px rgba(252, 182, 159, 0.5)';
        });
        
        document.querySelector('.hero-cta-demo').addEventListener('mouseleave', function() {
            this.style.transform = 'translateY(0) scale(1)';
            this.style.boxShadow = '0 4px 16px rgba(252, 182, 159, 0.3)';
        });
    </script>
</body>
</html>